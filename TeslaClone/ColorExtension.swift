import SwiftUI

extension Color {
    static let theme = ColorTheme()
}

struct ColorTheme {
    let green = Color("green")
    let red = Color("red")
    let blue = Color("blue")
    let background = Color("darkGray")
}
